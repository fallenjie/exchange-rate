# PRD-001 汇率换算工具

## 背景
用户需要一个汇率换算网页工具，支持输入金额后实时展示多个币种的换算结果。

## 原始需求
给我实现一个汇率换算的功能，对接实时汇率，能够按输入的金额自动换算出其他币种汇率

## 功能需求

### 核心功能

| 功能ID | 功能描述 | 优先级 | 备注 |
|--------|---------|--------|------|
| FUNC-01 | **汇率 API 对接** | 必须 | 使用 frankfurter.app（免费、无 key、支持 30+ 币种） |
| FUNC-02 | 金额输入 | 必须 | 用户输入待换算金额 |
| FUNC-03 | 币种选择 | 必须 | 支持 10 个主流币种，默认基准 USD |
| FUNC-04 | 换算结果展示 | 必须 | 实时换算（防抖 300ms） |

### 边界情况处理

| 场景 | 处理方式 | 提示文案 |
|------|---------|---------|
| 空输入 | 不触发请求，显示占位 | "请输入金额" |
| 非法字符输入 | 过滤非数字和小数点字符 | 无提示，静默过滤 |
| 负数 / 零 | 阻止换算 | "请输入正数" |
| 金额超限（> 999,999,999,999） | 阻止换算 | "金额过大，请重新输入" |
| 网络异常 / 超时 | 显示错误提示，尝试降级 | "汇率获取失败，请稍后重试" |
| API 不可用（503/429） | 显示缓存汇率 + 时间戳 | "当前数据来自缓存（更新时间：XX:XX）" |
| API 请求超时 | 超时 5s 中断请求，提示降级 | "汇率获取超时，请稍后重试" |

### UI 交互说明

#### 输入框
- placeholder="请输入金额"
- 辅助提示文字："支持最多2位小数"
- 最大长度：15位整数 + 2位小数
- 实时数字格式（千分位，如 1,234,567.89）

#### 结果展示
- 每个币种一个卡片
- 卡片内容：【币种符号 + 金额 + 币种名称】
- 例：$1,234.56 USD 美元

#### Loading 状态
- 换算区域显示骨架屏或 loading 动画
- 文案："正在获取汇率..."

#### 基准货币切换
- 下拉选择基准货币（默认 USD）
- 切换后自动重新计算所有币种

#### 精度切换
- 可切换显示 2 位小数 / 4 位小数
- 例：¥1,234.5678 CNY 人民币

#### 响应式设计
- 移动端（<768px）：单列布局
- 桌面端（≥768px）：2-3 列网格

#### 市场休市提示
- 周末/假日（非交易日）显示提示
- 文案："非交易日，数据可能延迟"

### 字段说明

#### 币种符号对照表

| 币种代码 | 符号 | 币种名称 |
|---------|------|---------|
| USD | $ | 美元 |
| EUR | € | 欧元 |
| GBP | £ | 英镑 |
| JPY | ¥ | 日元 |
| CNY | ¥ | 人民币 |
| HKD | $ | 港币 |
| KRW | ₩ | 韩元 |
| SGD | $ | 新加坡元 |
| AUD | $ | 澳元 |
| CAD | $ | 加元 |

#### 精度规则
- 统一保留 2 位小数（人民币 JPY 保留 3 位）
- 四舍五入处理

#### 数字格式
- 千分位分隔符（如 1,234,567.89）

### 缓存策略
- 汇率数据缓存 10 分钟
- 避免频繁请求 API，降低限速风险
- 缓存命中时直接使用，不发起网络请求

### 无障碍支持
- 键盘完全可导航（Tab / Shift+Tab）
- 输入框和按钮有清晰的 ARIA 标签
- 屏幕阅读器友好（语义化 HTML）
- 足够的颜色对比度

### API 降级方案
- frankfurter.app 不可用时（HTTP 503/429）
- 显示上次缓存的汇率 + 时间戳
- 提示用户数据可能非实时

### 非功能需求

| 需求类型 | 说明 |
|---------|------|
| 页面加载速度 | 首次加载 < 2s（纯前端，无后端） |
| Loading 状态 | API 请求期间显示 loading 动画或骨架屏 |
| 异常提示 | 网络异常、超时、API 不可用时有友好提示 |
| 缓存 | 汇率缓存 10 分钟，支持降级展示 |
| 无障碍 | 键盘导航、ARIA 标签、屏幕阅读器支持 |

## 用户故事
作为用户，我希望输入一个金额后快速看到其他币种的换算结果，以便日常参考实时汇率。

## 验收标准

| 验收项 | 通过条件 |
|--------|---------|
| AC-01 | 输入金额后能展示换算结果 |
| AC-02 | 汇率数据来源于实时 API |
| AC-03 | 支持主流币种换算（USD/EUR/GBP/JPY/CNY/HKD/KRW/SGD/AUD/CAD） |
| AC-04 | 异常情况有友好提示 |
| AC-05 | 空输入/负数/超限金额有明确提示 |
| AC-06 | 基准货币可切换，切换后自动重新计算 |

## 技术方案

| 项目 | 方案 |
|------|------|
| 汇率 API | frankfurter.app（免费，无需 key） |
| 基准货币 | USD（用户可切换） |
| 交互方式 | 实时换算，300ms 防抖 |
| 技术栈 | 纯前端 HTML/CSS/JS |
| 币种 | USD, EUR, GBP, JPY, CNY, HKD, KRW, SGD, AUD, CAD |
| API 超时 | 5 秒超时 |
| 缓存策略 | LocalStorage / 内存缓存 10 分钟 |
| UI 风格 | 深色主题（参考 oil-price-web）：背景 #0a0e14，卡片 #131b27，金色强调 #f0a500，DM Sans 字体 |

## 项目信息
- **Git 仓库**: D:\ai-dev-git\exchange-rate
- **GitHub Pages**: https://fallenjie.github.io/exchange-rate/
- **创建时间**: 2026-05-01
